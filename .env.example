# Google Places API Key
#
# To enable real restaurant data:
# 1. Go to https://console.cloud.google.com/apis/credentials
# 2. Create a new API key
# 3. Enable these APIs:
#    - Places API
#    - Maps SDK for iOS (for production)
#    - Maps SDK for Android (for production)
# 4. Copy the key below
# 5. Rename this file to .env
#
# Without this key, the app will use mock restaurant data (Buenos Aires)
EXPO_PUBLIC_GOOGLE_PLACES_API_KEY=your_api_key_here
